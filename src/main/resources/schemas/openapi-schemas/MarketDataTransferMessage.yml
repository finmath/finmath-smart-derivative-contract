type: object
required:
  - requestTimestamp
  - values
properties:
  requestTimestamp:
    type: string
    format: date-time
  values:
    type: array
    items:
      type: object
      properties:
        dataTimestamp:
          type: string
          format: date-time
        value:
          type: number
          format: double
        symbol:
          type: string